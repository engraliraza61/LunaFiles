@model IEnumerable<Luna.Recruitment.VisaProcessing.Data.Models.PermissionRequest>

@{
    ViewData["Title"] = "Permission";

}

@*@if (User.Identity.IsAuthenticated)
{
    if (User.IsInRole("HR-Permission-New"))
    {
<p>
    <a class="btn btn-primary" asp-action="Create" asp-route-OepvisaDemandId="@ViewData["OepvisaDemandId"]">Create New</a>
</p>
    }
}*@
<p>
    <a class="btn btn-primary" asp-action="Create" asp-route-OepvisaDemandId="@ViewData["OepvisaDemandId"]">Create New</a>
</p>
<table class="table applyDataTable">
    <thead>
        <tr>
            <th>File Number</th>
            <th>Sponser Name</th>
            <th>Sponser ID</th>
            <th>Counslate</th>
            <th>Permission Number</th>
            <th>Apply Date</th>
            <th>Receiving Date</th>
            <th>Expiry Date</th>
            <th>Visa Number</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.OepvisaDemand.Code)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OepvisaDemand.Sponser.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OepvisaDemand.Sponser.Code)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OepvisaDemand.Counslate.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PermissionNumber)
                </td>
                <td>
                    @Convert.ToDateTime(item.ApplyDate).ToString("dd/MM/yyyy")
                </td>
                <td>
                    @Convert.ToDateTime(item.ReceivingDate).ToString("dd/MM/yyyy")
                </td>
                <td>
                    @Convert.ToDateTime(item.ValidityDate).ToString("dd/MM/yyyy")
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OepvisaDemand.BatchNumber)
                </td>
                <td>
                    @*@if (User.Identity.IsAuthenticated)
        {
            if (User.IsInRole("HR-Permission-Edit"))
            {
        <a asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-edit"></i></a><span>|</span>
            }
        }
        @if (User.Identity.IsAuthenticated)
        {
            if (User.IsInRole("HR-Permission-Delete"))
            {
                <p>
                    <a asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash-alt"></i></a>
                </p><span>|</span>
            }
        }*@
                    <a asp-action="Edit"  asp-route-id="@item.Id"><i class="fas fa-edit"></i></a> |
                    <a asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash-alt"></i></a> |
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="Undertaking1">Undertaking 1</a> |
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="Undertaking2">Undertaking 2</a>
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="UndertakingForTest">UndertakingForTest</a> |
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="InterviewTestCenter">InterviewTestCenter</a> |
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="NewGovernmentOfPakistan">NewGovernmentOfPakistan</a> |
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="DemandLetter">Demand Letter</a> |
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="FinalPermission">Final Permissions</a>
                    <a asp-action="Report" target="_blank" asp-route-OEPVisaDemandId="@item.OepvisaDemandId" asp-route-report="AgencyUndertaking">Agency Undertaking</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<div class="col-md-12">
    <div class="form-group col-md-12 float-left">
        &nbsp; &nbsp;
        <a asp-controller="OEPVisaDemand" asp-action="Index" class="btn btn-info float-right">Demand List</a> &nbsp; &nbsp;
    </div>
</div>
